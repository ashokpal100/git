#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../jade-pdf-redline/bin/jade-pdf" "$@"
  ret=$?
else 
  node  "$basedir/../jade-pdf-redline/bin/jade-pdf" "$@"
  ret=$?
fi
exit $ret
